apiVersion: apps.open-cluster-management.io/v1
kind: PlacementRule
metadata:
  namespace: cassandra
  name: cassandra
  labels:
    app: cassandra
spec:
  clusterSelector:
    matchLabels:
      name: gabcchin-aws-passive
